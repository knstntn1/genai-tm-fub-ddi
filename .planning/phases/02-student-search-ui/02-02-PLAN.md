---
phase: 02-student-search-ui
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/addOpenVerseLocaleKeys.cjs
  - public/locales/de-DE/image_adv.json
  - public/locales/en-GB/image_adv.json
  - public/locales/fi-FI/image_adv.json
  - public/locales/fr-FR/image_adv.json
  - public/locales/it-IT/image_adv.json
  - public/locales/ja-JP/image_adv.json
  - public/locales/kr-KR/image_adv.json
  - public/locales/krl-FI/image_adv.json
  - public/locales/pt-BR/image_adv.json
  - public/locales/ru-RU/image_adv.json
  - public/locales/si-LK/image_adv.json
  - public/locales/sv/image_adv.json
  - public/locales/sw/image_adv.json
  - public/locales/tr-TR/image_adv.json
  - public/locales/ua-UA/image_adv.json
autonomous: true
requirements:
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08
  - TEST-03
must_haves:
  truths:
    - "German classroom-facing OpenVerse search copy is available through the image workflow namespace."
    - "Every existing locale `image_adv.json` file contains the same `trainingdata.openverse` key set so runtime locale switching does not fall back to missing keys."
    - "Locale copy supports class-scoped title, explicit search, loading, empty, retryable error, rate-limit, failed-use, result action, alt fallback, and pagination states."
  artifacts:
    - path: "public/locales/de-DE/image_adv.json"
      provides: "German OpenVerse search UI copy"
      contains: "trainingdata.openverse"
    - path: "public/locales/*/image_adv.json"
      provides: "Fallback-style OpenVerse search UI copy for every existing locale bundle"
      contains: "trainingdata.openverse"
  key_links:
    - from: "src/workflow/OpenVerseSearch/OpenVerseSearchDialog.tsx"
      to: "public/locales/*/image_adv.json"
      via: "useTranslation(namespace) keys under trainingdata.openverse"
      pattern: "trainingdata\\.openverse"
---

<objective>
Add the locale keys required by the Phase 2 OpenVerse search dialog to every existing image workflow locale bundle through a deterministic mechanical transform.

Purpose: Ensure UI-02 through UI-07 states and TEST-03 assertions have stable localized strings, with German classroom copy as the primary visual contract and English fallback-style copy for other locales where safe localized copy is not available.
Output: A one-off deterministic script plus updated `trainingdata.openverse` objects in all existing `public/locales/*/image_adv.json` files.
</objective>

<execution_context>
@/Users/oltmannk/.codex/get-shit-done/workflows/execute-plan.md
@/Users/oltmannk/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-student-search-ui/02-CONTEXT.md
@.planning/phases/02-student-search-ui/02-UI-SPEC.md
@.planning/phases/02-student-search-ui/02-RESEARCH.md
@.planning/phases/02-student-search-ui/02-PATTERNS.md
@public/locales/de-DE/image_adv.json
@public/locales/en-GB/image_adv.json

<interfaces>
The dialog component from Plan 01 will call keys under `trainingdata.openverse` in the active variant namespace. Add exactly these string keys to every existing `public/locales/*/image_adv.json` file:

```json
{
  "title": "OpenVerse: {{className}}",
  "searchLabel": "Suchbegriff",
  "searchPlaceholder": "z. B. Katze",
  "searchAction": "Bilder suchen",
  "useImage": "Dieses Bild nutzen",
  "initial": "Suche nach Bildern für diese Klasse.",
  "loading": "Suche Bilder...",
  "loadingMore": "Weitere Bilder werden geladen...",
  "emptyQuery": "Gib einen Suchbegriff ein.",
  "emptyTitle": "Keine Bilder gefunden.",
  "emptyBody": "Versuche einen anderen Suchbegriff.",
  "retryableError": "Die Bildsuche hat nicht geklappt. Bitte erneut versuchen.",
  "rateLimit": "Gerade sind zu viele Suchanfragen aktiv. Versuche es gleich noch einmal.",
  "retry": "Erneut versuchen",
  "more": "Mehr Ergebnisse",
  "failedUse": "Dieses Bild konnte nicht genutzt werden. Bitte erneut versuchen.",
  "fallbackAlt": "OpenVerse Bild"
}
```

For non-German locales, use English fallback-style copy unless the existing file language can be safely preserved without machine-translation guesswork. Do not add copy for visible license, attribution, creator, source, mature content, or advanced filters.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic locale propagation script</name>
  <files>scripts/addOpenVerseLocaleKeys.cjs</files>
  <action>Create `scripts/addOpenVerseLocaleKeys.cjs` as a deterministic one-off Node script. The script must enumerate existing `public/locales/*/image_adv.json` files, parse JSON, add or replace only `trainingdata.openverse`, preserve all sibling keys, write JSON with 4-space indentation plus trailing newline, use exact German copy for `de-DE`, and use the same clear English fallback-style copy for every non-German locale. Keep the locale source-of-truth objects inside the script so the 15-file JSON update is mechanically auditable. Do not add visible license, attribution, creator, source, mature-content, or advanced-filter copy.</action>
  <verify>
    <automated>node scripts/addOpenVerseLocaleKeys.cjs --check</automated>
  </verify>
  <done>The deterministic script exists, dry-run/check mode reports the expected locale files, and it owns the exact OpenVerse key set to be propagated.</done>
</task>

<task type="auto">
  <name>Task 2: Run locale propagation across all image workflow locale files</name>
  <files>public/locales/de-DE/image_adv.json, public/locales/en-GB/image_adv.json, public/locales/fi-FI/image_adv.json, public/locales/fr-FR/image_adv.json, public/locales/it-IT/image_adv.json, public/locales/ja-JP/image_adv.json, public/locales/kr-KR/image_adv.json, public/locales/krl-FI/image_adv.json, public/locales/pt-BR/image_adv.json, public/locales/ru-RU/image_adv.json, public/locales/si-LK/image_adv.json, public/locales/sv/image_adv.json, public/locales/sw/image_adv.json, public/locales/tr-TR/image_adv.json, public/locales/ua-UA/image_adv.json</files>
  <action>Run `node scripts/addOpenVerseLocaleKeys.cjs --write` to update every existing `public/locales/*/image_adv.json`. Do not manually edit each locale file. German `de-DE` must use the exact copy from the UI spec, including `Dieses Bild nutzen` and `Mehr Ergebnisse`. All other locales use the English fallback-style copy from the script because no product-reviewed translations are available in this phase. Keep valid formatted JSON with 4-space indentation.</action>
  <verify>
    <automated>node scripts/addOpenVerseLocaleKeys.cjs --check</automated>
    <automated>node -e "const fs=require('fs'); const keys=['title','searchLabel','searchPlaceholder','searchAction','useImage','initial','loading','loadingMore','emptyQuery','emptyTitle','emptyBody','retryableError','rateLimit','retry','more','failedUse','fallbackAlt']; const files=fs.readdirSync('public/locales').map((locale)=>'public/locales/'+locale+'/image_adv.json').filter((file)=>fs.existsSync(file)); for (const file of files) { const json=JSON.parse(fs.readFileSync(file,'utf8')); const openverse=json.trainingdata && json.trainingdata.openverse; if (!openverse) throw new Error(file+' missing trainingdata.openverse'); for (const key of keys) { if (typeof openverse[key] !== 'string' || openverse[key].trim().length === 0) throw new Error(file+' missing '+key); } } const de=JSON.parse(fs.readFileSync('public/locales/de-DE/image_adv.json','utf8')).trainingdata.openverse; if (de.useImage !== 'Dieses Bild nutzen') throw new Error('de-DE useImage mismatch'); if (de.more !== 'Mehr Ergebnisse') throw new Error('de-DE more mismatch'); console.log('validated '+files.length+' image_adv locale files');"</automated>
    <automated>npm run lint</automated>
    <automated>npm run build</automated>
  </verify>
  <done>Every existing `image_adv.json` locale has the complete OpenVerse key set, German copy matches the UI contract exactly, JSON parses cleanly, and lint still passes.</done>
</task>

<task type="auto">
  <name>Task 3: Verify locale scope excludes deferred metadata and filter UI</name>
  <files>scripts/addOpenVerseLocaleKeys.cjs, public/locales/de-DE/image_adv.json, public/locales/en-GB/image_adv.json, public/locales/fi-FI/image_adv.json, public/locales/fr-FR/image_adv.json, public/locales/it-IT/image_adv.json, public/locales/ja-JP/image_adv.json, public/locales/kr-KR/image_adv.json, public/locales/krl-FI/image_adv.json, public/locales/pt-BR/image_adv.json, public/locales/ru-RU/image_adv.json, public/locales/si-LK/image_adv.json, public/locales/sv/image_adv.json, public/locales/sw/image_adv.json, public/locales/tr-TR/image_adv.json, public/locales/ua-UA/image_adv.json</files>
  <action>Inspect the added `trainingdata.openverse` objects and ensure they contain only Phase 2 student search UI copy: class-scoped title, search field/action, use-image action, state messages, retry, pagination, failed-use, and fallback alt. Do not add visible copy for license, attribution, creator, source, provider provenance, file type, aspect ratio, mature content, safety filters, bulk import, or OpenVerse policy explanations.</action>
  <verify>
    <automated>node -e "const fs=require('fs'); const forbidden=/license|licence|attribution|creator|source|provenance|file type|filetype|aspect ratio|mature|filter|bulk|policy/i; const files=fs.readdirSync('public/locales').map((locale)=>'public/locales/'+locale+'/image_adv.json').filter((file)=>fs.existsSync(file)); for (const file of files) { const openverse=JSON.parse(fs.readFileSync(file,'utf8')).trainingdata.openverse; for (const [key,value] of Object.entries(openverse)) { if (forbidden.test(key) || forbidden.test(String(value))) throw new Error(file+' forbidden OpenVerse locale content in '+key); } } console.log('validated OpenVerse locale scope');"</automated>
  </verify>
  <done>Locale additions support the dialog states without introducing deferred metadata, attribution, or advanced-filter UI language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Locale JSON -> React UI | Static translation strings are loaded at runtime and rendered inside the workflow dialog. |
| Student-facing copy -> classroom users | Copy can imply scope, action semantics, import success, or unavailable filter/license features. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | I | `public/locales/*/image_adv.json` | mitigate | Do not add visible metadata, attribution, source, license, mature-content, or advanced-filter strings in `trainingdata.openverse`. |
| T-02-07 | R | result action copy | mitigate | Use neutral `Dieses Bild nutzen` / `Use this image`; avoid copy that claims the image was imported or added because Phase 3 performs actual mutation. |
| T-02-08 | D | runtime i18n lookup | mitigate | Add the same complete key set to every existing `image_adv.json` file and verify with a JSON/key validation script. |
| T-02-09 | S | class-scoped title copy | mitigate | Include `{{className}}` in `title` so the dialog can identify the current class context. |
</threat_model>

<verification>
Run:

```bash
node -e "const fs=require('fs'); const keys=['title','searchLabel','searchPlaceholder','searchAction','useImage','initial','loading','loadingMore','emptyQuery','emptyTitle','emptyBody','retryableError','rateLimit','retry','more','failedUse','fallbackAlt']; const files=fs.readdirSync('public/locales').map((locale)=>'public/locales/'+locale+'/image_adv.json').filter((file)=>fs.existsSync(file)); for (const file of files) { const json=JSON.parse(fs.readFileSync(file,'utf8')); const openverse=json.trainingdata && json.trainingdata.openverse; if (!openverse) throw new Error(file+' missing trainingdata.openverse'); for (const key of keys) { if (typeof openverse[key] !== 'string' || openverse[key].trim().length === 0) throw new Error(file+' missing '+key); } } const de=JSON.parse(fs.readFileSync('public/locales/de-DE/image_adv.json','utf8')).trainingdata.openverse; if (de.useImage !== 'Dieses Bild nutzen') throw new Error('de-DE useImage mismatch'); if (de.more !== 'Mehr Ergebnisse') throw new Error('de-DE more mismatch'); console.log('validated '+files.length+' image_adv locale files');"
node -e "const fs=require('fs'); const forbidden=/license|licence|attribution|creator|source|provenance|file type|filetype|aspect ratio|mature|filter|bulk|policy/i; const files=fs.readdirSync('public/locales').map((locale)=>'public/locales/'+locale+'/image_adv.json').filter((file)=>fs.existsSync(file)); for (const file of files) { const openverse=JSON.parse(fs.readFileSync(file,'utf8')).trainingdata.openverse; for (const [key,value] of Object.entries(openverse)) { if (forbidden.test(key) || forbidden.test(String(value))) throw new Error(file+' forbidden OpenVerse locale content in '+key); } } console.log('validated OpenVerse locale scope');"
npm run lint
npm run build
```
</verification>

<success_criteria>
Locale scope is complete when all existing image workflow locale files expose the complete `trainingdata.openverse` key set, German copy exactly matches the approved classroom UI contract, non-German locales have safe fallback-style copy, and no deferred metadata/attribution/filter language appears.
</success_criteria>

<output>
After completion, create `.planning/phases/02-student-search-ui/02-02-SUMMARY.md`.
</output>
